{
  "name": "fph-portal",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run index && npm run follow && astro build",
    "preview": "astro preview",
    "index": "node scripts/build-index.mjs",
    "validate": "node scripts/build-index.mjs --dry-run",
    "follow": "node scripts/follow-list.mjs",
    "exclude": "node scripts/exclude.mjs"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "marked": "^15.0.7",
    "yaml": "^2.7.0",
    "zod": "^3.24.2"
  }
}
